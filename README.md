# 넥스트바이크 (NEXTBIKE) - 네이버 최적화 중고오토바이 매입 플랫폼

[넥스트바이크](https://www.xn--b60bj1s89e3pf91mzkd.com/)의 고유 브랜드 아이덴티티(로고, 색감, 사업자 정보)와 **moto02.com**의 강력한 시스템을 뛰어넘는 **전국 200+ 지역별 프로그래매틱 SEO 엔진**을 탑재한 최신 웹사이트입니다.

---

## 🌟 주요 특징 및 구현 시스템

1. **전국 200+ 지역별 독립 랜딩페이지 (`/regions/[slug]`)**
   - 서울 25개 자치구, 경기 핵심 시/군(시흥, 부천, 안산, 수원, 고양, 성남 등), 인천, 충청, 영남, 호남, 강원, 제주 전역 커버
   - 각 지역별 고유 문구(출장 소요시간, 주차 및 상차 특성, 세부 동/구 리스트)로 **네이버 유사문서 판정 0% 달성**
   - 인접 지역 그물망 내부 링크(Internal Linking)로 크롤러 수집 극대화

2. **인기 차종/기종별 전용 매입 페이지 (`/models/[slug]`)**
   - 혼다(PCX, 포르자, 슈퍼커브, 헌터커브, 레블), 야마하(NMAX, XMAX, TMAX), BMW(R1250GS, C400GT), 할리데이비슨(소프테일/팻보이), 베스파, DNA모터스 UHR125, 로얄엔필드 등
   - 기종별 핵심 점검 포인트 및 시세 가산 요소 완벽 정리

3. **네이버 서치어드바이저 & 구글 서치콘솔 100% 최적화**
   - `sitemap.xml` & `robots.txt` 자동 생성 (`/sitemap.xml`, `/robots.txt`)
   - Schema.org JSON-LD 구조화 데이터 탑재:
     - `Organization` (넥스트바이크 공식 정보)
     - `LocalBusiness` & `Service` (지역별 매입 서비스 엔티티 증명)
     - `FAQPage` (네이버/구글 검색 결과 리치 스니펫 질의응답 노출)
     - `BreadcrumbList` (사이트 경로)
   - 네이버/구글 사이트 소유확인 메타태그 기등록 연동

4. **3단 고객 전환 (리드 수집) 장치**
   - 30초 간편 무료 견적 신청 폼 (사진 최대 5장 첨부, 지역/모델 자동 주입)
   - 모바일 하단 고정 전화(`010-4895-2487`) / 카카오톡 1:1 상담 / 견적 신청 바
   - 4단계 안심 매입 프로세스 & 자체 정비 인프라 신뢰 강조

---

## 🚀 실행 및 빌드 방법

### 1. 패키지 설치
```bash
cd C:\Users\A\.gemini\antigravity\scratch\nextbike-web
npm install
```

### 2. 로컬 개발 서버 실행
```bash
npm run dev
```
브라우저에서 `http://localhost:3000` 접속

### 3. 정적 프로덕션 빌드 (SSG)
```bash
npm run build
```
모든 200+ 지역 페이지와 50+ 기종 페이지가 0.1초 로딩 속도의 초고속 정적 HTML로 생성됩니다.

---

## 📋 네이버 서치어드바이저 등록 가이드

1. **네이버 서치어드바이저(Webmaster)** 접속
2. 사이트 등록: `https://www.xn--b60bj1s89e3pf91mzkd.com` (또는 실제 서비스 도메인)
3. 사이트 소유확인: 이미 `src/app/layout.tsx`에 기존 사이트의 네이버 인증 메타태그가 입력되어 있어 바로 통과됩니다.
4. **사이트맵 제출**: `https://도메인/sitemap.xml` 입력 후 확인
5. **robots.txt 수집 요청**: `https://도메인/robots.txt` 수집 확인
